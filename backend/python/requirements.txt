# Las dependencias reales están en la raíz del repositorio, que es lo que instala
# el Dockerfile y el postinstall. Este archivo solo delega para que dos copias no
# se desincronicen.
-r ../../requirements.txt
